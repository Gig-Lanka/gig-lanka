import EmptyState from '../../components/ui/EmptyState';

export default function BrowseScreen() {
  return <EmptyState className="bg-bg-main" message="Browsing gigs arrives in a later sprint." />;
}
