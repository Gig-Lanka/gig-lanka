import EmptyState from '../../components/ui/EmptyState';

export default function ProfileScreen() {
  return <EmptyState className="bg-bg-main" message="Your profile arrives in a later sprint." />;
}
