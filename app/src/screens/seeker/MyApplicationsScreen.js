import EmptyState from '../../components/ui/EmptyState';

export default function MyApplicationsScreen() {
  return (
    <EmptyState
      className="bg-bg-main"
      message="Tracking your applications arrives in a later sprint."
    />
  );
}
